# GitLab Contract Testing
contract-test:
  stage: test
  image: node:18-alpine
  script:
    - npm ci
    - npm run test:contract
    - npm run contract:report
  artifacts:
    reports:
      contract: contract-report.json
  only:
    - merge_requests
    - main
  tags:
    - contract


